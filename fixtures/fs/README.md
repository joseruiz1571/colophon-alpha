# Fixture filesystem

This directory is the synthetic filesystem the demo upstream's `fs.read`
and `fs.write` tools operate on. Nothing under `fixtures/` is real user
data; every value here is synthetic and every domain is `.example`.
